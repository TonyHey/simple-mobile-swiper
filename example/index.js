import swiper from "simple-mobile-swiper"

const swiperContainer = document.querySelector(".swiper_container")
const pagination = document.querySelector(".swiper_pagination")
swiper(swiperContainer, { pagination })
